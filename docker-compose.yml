version: '3.8'
services:
  scanner:
    build: .
    container_name: spx-scanner
    restart: unless-stopped
    ports:
      - "5000:5000"
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
